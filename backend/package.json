{
  "name": "bakend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:win": "node -e \"if (process.platform === 'win32') process.exit(1)\" || , if exist dist rmdir /Q /S dist",
    "clean:lin": "node -e \"if (process.platform !== 'win32') process.exit(1)\" || rm -rf dist",
    "dev": "nodemon src/index.ts",
    "clean": "npm run clean:win && npm run clean:lin && tsc",
    "build": "npm run clean:win && npm run clean:lin && tsc",
    "vercel-dev": "node dist/index.js"
  },
  "keywords": [],
  "author": "Mampionona",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.17.1",
    "mongoose": "^7.0.3",
    "passport": "^0.6.0",
    "passport-facebook": "^3.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.13.0",
    "@vercel/node": "^2.10.0",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
